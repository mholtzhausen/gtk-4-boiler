//! ToastStack — a stack of auto-dismissing notifications.

/// The visual kind of a toast notification.
pub enum ToastKind {
    Success,
    Error,
    Warning,
    Info,
}

/// A stack of toast notifications overlaid on content.
pub struct ToastStack {
    // Placeholder
}
